# 순차 탐색(Sequential search)
- List 안에 있는 특정한 값을 찾기 위해 앞에서부터 하나씩 차례로 확인하는 방식
- 시간복잡도 : O(N)

# 이진 탐색(Binary search)
- 배열 내부가 정렬되어 있어야함
- 정렬되어 있다면, 매우 빠르게 탐색 가능
- 시작점, 끝점 그리고 중간점. 3개의 변수 사용
- 찾으려는 값과 중간점(middle) 위치에 있는 값들을 반복적으로 비교
- 시간복잡도 : O(logN)